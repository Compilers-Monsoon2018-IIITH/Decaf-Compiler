class BubbleSort
{
	void main()
	{
		int N, i, j,temp;
		int a[100];
		N_1 = N-1;

		//This section for initialising the array
		N = 50;
		for i=0,N
		{
			a[i] = (N - i);
		}

		//Sorting the array

		for i=0,N
		{
			for j=1,N_1
			{
				if(a[j] > a[j+1])
				{
					temp = a[j];
					a[j] = a[j+1];
					a[j+1] = temp;
				}
			}
		}

		//Printing the array

		for i=0,N
		{
			callout("print_int",a[i]);
		}	
	}
}